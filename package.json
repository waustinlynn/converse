{
  "name": "converse",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "ui",
    "backend",
    "infra"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev:ui": "npm run dev -w ui",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w ui && npm run build -w backend"
  },
  "dependencies": {
    "@aws-sdk/client-sts": "^3.1053.0"
  }
}
